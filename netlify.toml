[build]
  command = "rm -rf node_modules && npm ci --include=optional && npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=optional"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
